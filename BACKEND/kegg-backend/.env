# ============================================================
# K-EGG BACKEND ENVIRONMENT VARIABLES
# ============================================================

# MongoDB Connection (Atlas)
MONGO_URI=mongodb+srv://K-eggDB:MAYSAN@cluster0.ydqpgie.mongodb.net/kegg_db?retryWrites=true&w=majority

# Server port
# Render/Railway will set PORT automatically — leave as 5000 for local
PORT=5000

# Environment
NODE_ENV=development
# Change to "production" when deploying live
